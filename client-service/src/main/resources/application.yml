server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: client-service
  sleuth:
    sampler:
      probability: 2.0

feign:
  circuitbreaker:
    enabled: true

